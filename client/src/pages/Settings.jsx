import classes from "./Settings.module.css";

function Settings() {
    return ( 
        <div className={classes.settings}>
            <h1>Settings page is rendered here</h1>
        </div>
     );
}

export default Settings;